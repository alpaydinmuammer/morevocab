import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('More Vocab app smoke test', (WidgetTester tester) async {
    // Basic smoke test - just verify the app can be imported
    expect(true, isTrue);
  });
}
